<!DOCTYPE html>
<html>
<head>
  <title>Car Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    h1 {
      margin-bottom: 20px;
    }
    form {
      background-color: #f4f4f4;
      padding: 20px;
      border-radius: 5px;
    }

    .form-group {
      margin-bottom: 20px; /* Add margin between form groups */
    }

    label {
      font-weight: bold;
    }
    input[type="checkbox"] {
      margin-right: 10px;
    }
    input[type="text"],
    textarea {
      width: 100%;
      padding: 5px;
      margin: 5px 0;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
    button[type="submit"] {
      background-color: #1464c3;
      color: white;
      border: none;
      padding: 10px 20px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Car Details</h1>
    <form method="post" action="/updateCar">
      <h2>Car: <%= item.license %></h2>
      <div class="form-group">
        <label for="depType">Deposit Type:</label>
        <div class="input-field">
          <input type="checkbox" name="depType" value="1"> Tipo 1 (TOYOTA AYGO)
          <input type="checkbox" name="depType" value="2"> Tipo 2 (HYUNDAI I20)
          <input type="checkbox" name="depType" value="3"> Tipo 3 (RENAULT CLIO 900)
          <input type="checkbox" name="depType" value="4"> Tipo 4 (NISSAN JUKE)
          <input type="checkbox" name="depType" value="5"> Tipo 5 (VOLKSWAGEN CADDY MAXI)
          <!-- Add more options as needed -->
        </div>
      </div>
      
      <div class="form-group">
        <label for="trunkType">Trunk Type:</label>
        <div class="input-field">
          <input type="checkbox" name="trunkType" value="1"> Tipo 1 (HYUNDAI I20)
          <input type="checkbox" name="trunkType" value="2"> tipo 2 (TOYOTA YARIS 1.3 A/A)
          <input type="checkbox" name="trunkType" value="3"> tipo 3 (VOLKSWAGEN POLO 1.4)
          <input type="checkbox" name="trunkType" value="4"> tipo 4 (SEAT IBIZA 1.4 A/A)
          <input type="checkbox" name="trunkType" value="5"> tipo 5 (OPEL CORSA ESSENTIA)
          <input type="checkbox" name="trunkType" value="6"> tipo 6 (RENAULT CLIO 900)
          <input type="checkbox" name="trunkType" value="7"> tipo 7 (RENAULT CLIO SPORT TOURER)
          <input type="checkbox" name="trunkType" value="8"> tipo 8 (RENAULT CAPTURE)
          <input type="checkbox" name="trunkType" value="9"> tipo 9 (NISSAN JUKE)
          <input type="checkbox" name="trunkType" value="10"> tipo 10 (RENAULT MEGANE SPORT TOURER)
          <input type="checkbox" name="trunkType" value="11"> tipo 11 (CITROEN BERLINGO)
          <input type="checkbox" name="trunkType" value="12"> tipo 12 (RENAULT CLIO HYBRIDO AUTOMATICO)
          <input type="checkbox" name="trunkType" value="13"> tipo 13 (BMW AUTOMATICO)
          <input type="checkbox" name="trunkType" value="14"> tipo 14 (VOLKSWAGEN CADDY MAXI)
          <input type="checkbox" name="trunkType" value="15"> tipo 15 (BMW X2 AUTOMATICO)
          <!-- Add more options as needed -->
        </div>
      </div>
      
      <div class="form-group">
        <label for="revType">Reverse Type:</label>
        <div class="input-field">
          <input type="checkbox" name="revType" value="1"> Tipo 1 (HYUNDAI I20)
          <input type="checkbox" name="revType" value="2"> Tipo 2 (NISSAN JUKE)
          <input type="checkbox" name="revType" value="3"> Tipo 3 (SEAT IBIZA 1.4 A/A)
          <input type="checkbox" name="revType" value="4"> Tipo 4 (RENAULT CLIO HYBRIDO AUTOMATICO)
          <input type="checkbox" name="revType" value="5"> Tipo 5 (BMW BMW 218D ACTIVE TOURER)

          <!-- Add more options as needed -->
        </div>
      </div>

      <div class="form-group">
        <label for="carImages">Car Images:</label>
        <div class="input-field">
          <input type="file" name="carImages" accept="image/*" multiple>
        </div>
      </div>

      <div class="form-group">
        <label for="notes">Notes:</label>
        <div class="input-field">
          <textarea name="notes" rows="4"></textarea>
        </div>
      </div>

      <div>
        <input type="hidden" name="id" id="id" class="input" value="<%= item._id %>"/>
        <input type="hidden" class="objectId" value="<%= item._id %>"> 
        <button type="submit" class="save-car" data-id="<%= item._id %>">Enviar</button>
      </div>
    </form>
  </div>
  <script>
    const detailsButtons = document.querySelectorAll('.save-car');
  
    detailsButtons.forEach((button) => {
      button.addEventListener('click', async () => {
        const objectId = button.getAttribute('data-id');
        const formData = new FormData(document.querySelector('form'));
  
        console.log(formData)

      });
    });
  </script>
</body>
</html>
